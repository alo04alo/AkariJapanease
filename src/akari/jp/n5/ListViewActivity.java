package akari.jp.n5;import java.util.ArrayList;import java.util.HashMap;import akari.jp.base.Question;import akari.jp.base.QuestionHandler;import akari.jp.n5.R;import akari.jp.utils.LazyAdapter;import android.app.Activity;import android.os.Bundle;import android.view.View;import android.widget.AdapterView;import android.widget.AdapterView.OnItemClickListener;import android.widget.ListView;public class ListViewActivity extends Activity {	QuestionHandler questionHandler;	Question[] questions;	public static final String KEY_QUESTION = "question";	public static final String KEY_CHOOSE = "choose";	public static final String KEY_RESULT = "result";	public static final String KEY_ID = "ID";	public static final String KEY_ICON_RESULT = "ICON";	ListView list;	LazyAdapter adapter;	@Override	public void onCreate(Bundle savedInstanceState) {		super.onCreate(savedInstanceState);		setContentView(R.layout.listview_layout);		questions = ((N5Support) getApplication()).getQuestions();		questionHandler = ((N5Support) getApplication()).getQuestionHandler();		ArrayList<HashMap<String, String>> songsList = new ArrayList<HashMap<String, String>>();		for (int i = 0; i < questions.length; i++) {			HashMap<String, String> map = new HashMap<String, String>();			map.put(KEY_ID, String.valueOf(i + 1));			map.put(KEY_CHOOSE,					questions[i].getContentofAnswer(questions[i].getChoose()));			map.put(KEY_RESULT,					questions[i].getContentofAnswer(questions[i].getResult()));			map.put(KEY_QUESTION, questions[i].getContent());			if (questions[i].getChoose() == questions[i].getResult())				map.put(KEY_ICON_RESULT, "TRUE");			else				map.put(KEY_ICON_RESULT, "FALSE");			songsList.add(map);		}		list = (ListView) findViewById(R.id.list);		adapter = new LazyAdapter(this, songsList);		list.setAdapter(adapter);		// Click event for single list row		list.setOnItemClickListener(new OnItemClickListener() {			@Override			public void onItemClick(AdapterView<?> parent, View view,					int position, long id) {			}		});	}}